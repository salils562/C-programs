#include <iostream>
using namespace std;

int main()
{
    /* int n;
     cout << "Enter n: ";
     cin >> n;
     int *arr = new int[n];
     for (int i = 0; i < n; i++)
     {
         cin >> arr[i];
     }
     for (int i = 0; i < n; i++)
         cout << "The element no " << (i + 1) << " is " << arr[i] << endl;
     This is how to dynamically allocate the array */
     /* 
     This is how to delete the dynamically allocated array
     int *arr=new int[3];
     arr[0]=20;
     arr[1]=20;
     arr[2]=20;
     delete[] arr;
     cout<<arr[0]<<endl;
     cout<<arr[1]<<endl;
     cout<<arr[2]<<endl;
     */


    return 0;
}